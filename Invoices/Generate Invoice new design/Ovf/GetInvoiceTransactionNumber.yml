info:
  name: GetInvoiceTransactionNumber
  type: http
  seq: 7

http:
  method: GET
  url: "{{currentIP}}/api/GenerateInvoice/GetInvoiceTransactionNumber"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
