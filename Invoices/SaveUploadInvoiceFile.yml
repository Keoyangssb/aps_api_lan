info:
  name: SaveUploadInvoiceFile
  type: http
  seq: 14

http:
  method: POST
  url: http://localhost:5236/api/Invoice/SaveUploadInvoiceFile?transaction_id=12025073102571622
  params:
    - name: transaction_id
      value: "12025073102571622"
      type: query
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value:
          - /D:/AIDC_Tech/APS/Docs/Invoices - Copy.xlsx
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
