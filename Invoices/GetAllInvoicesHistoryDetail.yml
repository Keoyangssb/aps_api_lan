info:
  name: GetAllInvoicesHistoryDetail
  type: http
  seq: 27

http:
  method: GET
  url: "{{currentIP}}/api/Invoice/GetAllInvoicesHistoryDetail"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
