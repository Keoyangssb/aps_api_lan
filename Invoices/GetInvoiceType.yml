info:
  name: GetInvoiceType
  type: http
  seq: 26

http:
  method: GET
  url: "{{currentIP}}/api/Invoice/GetInvoiceType"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
