info:
  name: GetOvfRate
  type: http
  seq: 7

http:
  method: GET
  url: "{{currentIP}}/api/General/GetOvfRate?mtow=50"
  params:
    - name: mtow
      value: "50"
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
