info:
  name: GetRevisedInvoiceAwaitingApproval
  type: http
  seq: 1

http:
  method: POST
  url: "{{currentIP}}/api/Invoice/GetRevisedInvoiceAwaitingApproval"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
