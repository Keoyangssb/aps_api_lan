info:
  name: TransactionAmountComparison
  type: http
  seq: 2

http:
  method: POST
  url: "{{currentIP}}/api/DashboardBOLLanding/TransactionAmountComparison"
  body:
    type: json
    data: |-
      {
        "year": "2026"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
