info:
  name: GetPurposeOfFlightList Copy
  type: http
  seq: 3

http:
  method: GET
  url: "{{currentIP}}/api/MasterData/GetPurposeOfFlightList"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
