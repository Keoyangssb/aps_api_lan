info:
  name: GetMatchingStatusReport
  type: http
  seq: 17

http:
  method: POST
  url: "{{currentIP}}/api/Reports/GetMatchingStatusReport"
  body:
    type: json
    data: "{\r

      \  \"from_date\": \"2025-01-01\",\r

      \  \"to_date\": \"2025-12-31\"\r

      }"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
