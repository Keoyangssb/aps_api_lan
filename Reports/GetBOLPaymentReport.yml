info:
  name: GetBOLPaymentReport
  type: http
  seq: 24

http:
  method: POST
  url: "{{currentIP}}/api/Reports/GetBOLPaymentReport"
  body:
    type: json
    data: "{\r

      \  \"page_number\": 1,\r

      \  \"page_size\": 10,\r

      \  \"from_date\": \"2025-01-01\",\r

      \  \"to_date\": \"2025-08-30\"\r

      }"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
