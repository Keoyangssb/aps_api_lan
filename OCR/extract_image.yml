info:
  name: extract_image
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5236/api/Ocr/extract
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value:
          - /C:/Users/ASUS/Pictures/aidc/11111.pdf
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
