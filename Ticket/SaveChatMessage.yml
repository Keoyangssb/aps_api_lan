info:
  name: SaveChatMessage
  type: http
  seq: 3

http:
  method: POST
  url: "{{currentIP}}/api/Ticket/SaveChatMessage"
  body:
    type: json
    data: "{\r

      \  \"id\": \"7\",\r

      \  \"message\": \"thank you for reporting. we're checking on it and lets you knows as soon\"\r

      }"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
