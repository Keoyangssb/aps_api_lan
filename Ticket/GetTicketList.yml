info:
  name: GetTicketList
  type: http
  seq: 1

http:
  method: POST
  url: "{{currentIP}}/api/Ticket/GetTicketList"
  body:
    type: json
    data: "{\r

      \  \"page_number\": 1,\r

      \  \"page_size\": 10,\r

      \  \"status_name\": \"\",\r

      \  \"category_id\": \"\",\r

      \  \"from_date\": \"\",\r

      \  \"to_date\": \"\"\r

      }"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
