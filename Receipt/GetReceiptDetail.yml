info:
  name: GetReceiptDetail
  type: http
  seq: 3

http:
  method: POST
  url: "{{currentIP}}/api/Receipt/GetReceiptDetail"
  body:
    type: json
    data: "{\r

      \  \"paymentCode\": \"2025040610272722\"\r

      }"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
