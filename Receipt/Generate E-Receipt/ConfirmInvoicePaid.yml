info:
  name: ConfirmInvoicePaid
  type: http
  seq: 4

http:
  method: POST
  url: "{{currentIP}}/api/Tracking/ConfirmInvoicePaid"
  body:
    type: json
    data: "{\r\n  \"id\": \"2714\"\r\n}"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
