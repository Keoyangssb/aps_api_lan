info:
  name: generate_e_receipt
  type: http
  seq: 4

http:
  method: POST
  url: "{{currentIP}}/api/Receipt/generate_e_receipt"
  body:
    type: json
    data: "{\r

      \  \"transaction_id\": \"1080\"\r

      }"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
