info:
  name: GetReceiptList
  type: http
  seq: 2

http:
  method: GET
  url: "{{currentIP}}/api/Receipt/GetReceiptList"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
