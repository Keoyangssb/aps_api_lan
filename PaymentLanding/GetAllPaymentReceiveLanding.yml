info:
  name: GetAllPaymentReceiveLanding
  type: http
  seq: 1

http:
  method: POST
  url: "{{currentIP}}/api/PaymentLanding/GetAllPaymentReceiveLanding"
  body:
    type: json
    data: "{\r

      \  \"pageNumber\": 1,\r

      \  \"pageSize\": 10,\r

      \  \"keywordSearch\": \"\"\r

      }"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
