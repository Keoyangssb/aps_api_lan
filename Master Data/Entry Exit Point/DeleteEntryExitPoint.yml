info:
  name: DeleteEntryExitPoint
  type: http
  seq: 2

http:
  method: POST
  url: "{{currentIP}}/api/FlightPermission/DeleteEntryExitPoint?id=119"
  params:
    - name: id
      value: "119"
      type: query
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
