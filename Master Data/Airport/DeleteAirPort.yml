info:
  name: DeleteAirPort
  type: http
  seq: 4

http:
  method: POST
  url: "{{currentIP}}/api/MasterData/DeleteAirPort?id=9"
  params:
    - name: id
      value: "9"
      type: query
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
