info:
  name: GetAirPortList
  type: http
  seq: 3

http:
  method: GET
  url: "{{currentIP}}/api/MasterData/GetAirPortList"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
