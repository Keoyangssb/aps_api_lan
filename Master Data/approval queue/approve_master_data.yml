info:
  name: approve_master_data
  type: http
  seq: 2

http:
  method: GET
  url: "{{currentIP}}/api/MasterData/approve_master_data"
  body:
    type: json
    data: |-
      {
        "category_code": "",
        "id": "",
        "remark": ""
      }
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
