info:
  name: reject_master_data
  type: http
  seq: 3

http:
  method: GET
  url: "{{currentIP}}/api/MasterData/reject_master_data"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
