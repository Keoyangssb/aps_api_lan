info:
  name: GetAircraftTypeList
  type: http
  seq: 5

http:
  method: POST
  url: "{{currentIP}}/api/MasterData/AddAircraftType"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
