info:
  name: DeleteAircraft
  type: http
  seq: 3

http:
  method: POST
  url: "{{currentIP}}/api/MasterData/DeleteAircraft?id=4"
  params:
    - name: id
      value: "4"
      type: query
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
