info:
  name: DeleteOverFlightRate
  type: http
  seq: 2

http:
  method: POST
  url: "{{currentIP}}/api/FlightPermission/DeleteOverFlightRate?id=10"
  params:
    - name: id
      value: "10"
      type: query
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
