info:
  name: GetCallSignList
  type: http
  seq: 5

http:
  method: GET
  url: "{{currentIP}}/api/MasterData/GetCallSignList"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
