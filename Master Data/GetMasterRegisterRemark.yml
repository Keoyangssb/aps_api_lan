info:
  name: GetMasterRegisterRemark
  type: http
  seq: 5

http:
  method: GET
  url: "{{currentIP}}/api/MasterData/GetMasterRegisterRemark"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
