info:
  name: GetApproveInvoiceLandingHistoryH Copy
  type: http
  seq: 12

http:
  method: GET
  url: http://localhost:5236/api/InvoiceLanding/GetApproveInvoiceLandingHistoryH
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
