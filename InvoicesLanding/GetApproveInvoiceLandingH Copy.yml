info:
  name: GetApproveInvoiceLandingH Copy
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:5236/api/InvoiceLanding/GetApproveInvoiceLandingH
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
