info:
  name: GetInvoiceLandingHistoryListH
  type: http
  seq: 3

http:
  method: GET
  url: "{{currentIP}}/api/InvoiceLanding/GetInvoiceLandingHistoryListH?fromDate=2023-01-01&toDate=2026-01-30"
  params:
    - name: fromDate
      value: 2023-01-01
      type: query
    - name: toDate
      value: 2026-01-30
      type: query
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
