info:
  name: GetApproveInvoiceLandingHistoryH
  type: http
  seq: 11

http:
  method: GET
  url: "{{currentIP}}/api/InvoiceLanding/GetApproveInvoiceLandingHistoryH"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
