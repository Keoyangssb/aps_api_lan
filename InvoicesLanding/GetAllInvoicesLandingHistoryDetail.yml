info:
  name: GetAllInvoicesLandingHistoryDetail
  type: http
  seq: 6

http:
  method: GET
  url: "{{currentIP}}/api/InvoiceLanding/GetAllInvoicesLandingHistoryDetail"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
