info:
  name: getAirlineName_by_callsign2
  type: http
  seq: 6

http:
  method: GET
  url: "{{currentIP}}/api/FlightMovement/getAirlineName_by_callsign?callsign=ABL726"
  params:
    - name: callsign
      value: ABL726
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
