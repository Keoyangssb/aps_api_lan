info:
  name: getAirlineName_by_callsign
  type: http
  seq: 6

http:
  method: POST
  url: "{{currentIP}}/api/FlightMovementLanding/getAirlineName_by_callsign?callsign="
  params:
    - name: callsign
      value: ""
      type: query
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
