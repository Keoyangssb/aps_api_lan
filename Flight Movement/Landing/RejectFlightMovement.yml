info:
  name: RejectFlightMovement
  type: http
  seq: 8

http:
  method: POST
  url: "{{currentIP}}/api/FlightMovementLanding/RejectFlightMovement"
  body:
    type: json
    data: |-
      {
          "active_flight_plan_id": "0",
      }
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
