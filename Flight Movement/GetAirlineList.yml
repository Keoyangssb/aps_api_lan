info:
  name: GetAirlineList
  type: http
  seq: 2

http:
  method: GET
  url: "{{currentIP}}/api/FlightMovement/GetAirlineList"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
