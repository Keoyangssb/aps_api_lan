info:
  name: get_departure_destination_list
  type: http
  seq: 3

http:
  method: GET
  url: "{{currentIP}}/api/FlightMovement/get_departure_destination_list"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
