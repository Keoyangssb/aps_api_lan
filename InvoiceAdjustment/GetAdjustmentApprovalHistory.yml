info:
  name: GetAdjustmentApprovalHistory
  type: http
  seq: 9

http:
  method: POST
  url: "{{currentIP}}/api/Invoice/GetAdjustmentApprovalHistory"
  body:
    type: json
    data: "{\r\n    \"id\": \"8\"\r\n}"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
