info:
  name: GetInvoiceAdjustmentToApprove
  type: http
  seq: 6

http:
  method: GET
  url: "{{currentIP}}/api/Invoice/GetInvoiceAdjustmentToApprove"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
