info:
  name: ConfirmPayment
  type: http
  seq: 25

http:
  method: POST
  url: "{{currentIP}}/api/Payment/ConfirmPayment"
  body:
    type: json
    data: "{\r\n    \"paymentId\": \"15\"\r\n}"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
