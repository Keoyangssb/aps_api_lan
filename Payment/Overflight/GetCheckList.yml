info:
  name: GetCheckList
  type: http
  seq: 28

http:
  method: POST
  url: "{{currentIP}}/api/Payment/GetCheckList"
  body:
    type: json
    data: "{\r\n  \"payment_id\": \"21\"\r\n}"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
