info:
  name: GetPaymentListToApproval
  type: http
  seq: 10

http:
  method: GET
  url: "{{currentIP}}/api/Payment/GetPaymentListToApproval"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
