info:
  name: GetPaymentStatus
  type: http
  seq: 31

http:
  method: GET
  url: "{{currentIP}}/api/Payment/GetPaymentStatus"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
