info:
  name: CancelPayment
  type: http
  seq: 37

http:
  method: POST
  url: "{{currentIP}}/api/PaymentLanding/CancelPayment"
  body:
    type: json
    data: "{\r\n    \"paymentId\": \"15\"\r\n}"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
