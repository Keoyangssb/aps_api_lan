info:
  name: GetDepartment_na
  type: http
  seq: 3

http:
  method: GET
  url: "{{currentIP}}/api/User/GetDepartment_na"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
