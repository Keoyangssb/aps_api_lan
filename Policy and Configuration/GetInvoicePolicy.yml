info:
  name: GetInvoicePolicy
  type: http
  seq: 4

http:
  method: GET
  url: "{{currentIP}}/api/PolicyAndConfiguration/GetInvoicePolicy"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
