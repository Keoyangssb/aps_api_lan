info:
  name: GetStaffPolicy
  type: http
  seq: 5

http:
  method: GET
  url: "{{currentIP}}/api/PolicyAndConfiguration/GetStaffPolicy"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
