info:
  name: GetFlightPermissionServiceCharge
  type: http
  seq: 2

http:
  method: GET
  url: "{{currentIP}}/api/PolicyAndConfiguration/GetFlightPermissionServiceCharge"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
