info:
  name: GetInvoicePenalty
  type: http
  seq: 6

http:
  method: GET
  url: "{{currentIP}}/api/PolicyAndConfiguration/GetInvoicePenalty"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
