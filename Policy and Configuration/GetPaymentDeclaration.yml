info:
  name: GetPaymentDeclaration
  type: http
  seq: 8

http:
  method: GET
  url: "{{currentIP}}/api/PolicyAndConfiguration/GetPaymentDeclaration"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
