info:
  name: GetFlightMovementServiceCharge
  type: http
  seq: 3

http:
  method: GET
  url: "{{currentIP}}/api/PolicyAndConfiguration/GetFlightMovementServiceCharge"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
