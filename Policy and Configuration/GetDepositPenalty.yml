info:
  name: GetDepositPenalty
  type: http
  seq: 7

http:
  method: GET
  url: "{{currentIP}}/api/PolicyAndConfiguration/GetDepositPenalty"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
