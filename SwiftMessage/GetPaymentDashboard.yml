info:
  name: GetPaymentDashboard
  type: http
  seq: 2

http:
  method: POST
  url: "{{currentIP}}/api/SwiftMessages/GetPaymentDashboard"
  body:
    type: json
    data: "{\r

      \    \"from_date\": \"\",\r

      \    \"to_date\": \"\"\r

      }"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
