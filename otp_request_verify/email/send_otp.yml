info:
  name: send_otp
  type: http
  seq: 2

http:
  method: POST
  url: "{{currentIP}}/api/User/RequestOtp"
  body:
    type: json
    data: |-
      {
        "userEmail": "keoyang.l.aidctech@gmail.com"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
