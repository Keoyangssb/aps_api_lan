info:
  name: GetSystemLog
  type: http
  seq: 35

http:
  method: POST
  url: "{{currentIP}}/api/General/GetSystemLog"
  body:
    type: json
    data: "{\r

      \  \"from_date\": \"\",\r

      \  \"to_date\": \"\"\r

      }"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
