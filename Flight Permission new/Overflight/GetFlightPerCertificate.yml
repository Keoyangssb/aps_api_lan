info:
  name: GetFlightPerCertificate
  type: http
  seq: 9

http:
  method: POST
  url: "{{currentIP}}/api/FlightPermission/GetFlightPerCertificate"
  body:
    type: json
    data: "{\r\n  \"flight_per_id\": \"37\"\r\n}"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
