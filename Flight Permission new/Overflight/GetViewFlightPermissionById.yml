info:
  name: GetViewFlightPermissionById
  type: http
  seq: 5

http:
  method: POST
  url: "{{currentIP}}/api/FlightPermission/GetViewFlightPermissionById"
  body:
    type: json
    data: "{\r\n  \"flight_per_id\": \"24\"\r\n}"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
