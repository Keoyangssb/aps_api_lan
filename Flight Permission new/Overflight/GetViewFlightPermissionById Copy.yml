info:
  name: GetViewFlightPermissionById Copy
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:5236/api/FlightPermission/GetViewFlightPermissionById
  body:
    type: json
    data: "{\r\n  \"flight_per_id\": \"50\"\r\n}"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
