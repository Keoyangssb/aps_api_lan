info:
  name: GetFlightPermissionMovementDetail
  type: http
  seq: 15

http:
  method: POST
  url: http://localhost:4001/api/FlightPermission/GetFlightPermissionMovementDetail
  body:
    type: json
    data: |-
      {
        "flight_per_id": "900"
      }
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
