info:
  name: GetViewFlightPermissionById Copy_1
  type: http
  seq: 8

http:
  method: POST
  url: http://localhost:5236/api/FlightPermission/GetViewFlightPermissionById
  body:
    type: json
    data: "{\r\n  \"flight_per_id\": \"23\"\r\n}"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
