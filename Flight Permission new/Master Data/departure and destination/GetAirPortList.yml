info:
  name: GetAirPortList
  type: http
  seq: 12

http:
  method: GET
  url: "{{currentIP}}/api/MasterData/GetAirPortList"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
