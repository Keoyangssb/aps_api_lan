info:
  name: GetFlightPerStatusList
  type: http
  seq: 4

http:
  method: GET
  url: "{{currentIP}}/api/MasterData/GetFlightPerStatusList"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
