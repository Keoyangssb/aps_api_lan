info:
  name: GetAllUser
  type: http
  seq: 1

http:
  method: GET
  url: "{{currentIP}}/api/User/GetAllUserActive"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
