info:
  name: AddUserRegisterApprovalSetting
  type: http
  seq: 3

http:
  method: POST
  url: "{{currentIP}}/api/ApprovalSetting/AddUserRegisterApprovalSetting"
  body:
    type: json
    data: "{\r

      \  \"approverId\": \"1\",\r

      \  \"approveLevel\": \"1\"\r

      }"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
