info:
  name: GetPaymentApprovalSettingList Copy
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:5236/api/ApprovalSetting/GetPaymentApprovalSettingList
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
