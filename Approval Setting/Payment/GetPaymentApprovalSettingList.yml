info:
  name: GetPaymentApprovalSettingList
  type: http
  seq: 1

http:
  method: GET
  url: "{{currentIP}}/api/ApprovalSetting/GetPaymentApprovalSettingList"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
