info:
  name: GetAllUserLand
  type: http
  seq: 1

http:
  method: GET
  url: "{{currentIP}}/api/ApproverSetting/GetAllUserLand"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
