info:
  name: DeleteFPApprovalSetting
  type: http
  seq: 5

http:
  method: POST
  url: "{{currentIP}}/api/ApproverSetting/DeleteFPApprovalSetting"
  body:
    type: json
    data: "{\r\n  \"id\": \"5\"\r\n}"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
