info:
  name: GetFPApprovalSettingList
  type: http
  seq: 2

http:
  method: GET
  url: "{{currentIP}}/api/ApproverSetting/GetFPApprovalSettingList"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
