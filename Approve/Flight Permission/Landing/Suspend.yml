info:
  name: Suspend
  type: http
  seq: 8

http:
  method: POST
  url: "{{currentIP}}/api/FlightPermissionLanding/Suspend"
  body:
    type: json
    data: "{\r

      \  \"flight_per_id\": \"1\",\r

      \  \"remark\": \"test\"\r

      }"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
