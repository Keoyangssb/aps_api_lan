info:
  name: CancalRequest
  type: http
  seq: 4

http:
  method: POST
  url: "{{currentIP}}/api/FlightPermission/CancalRequest"
  body:
    type: json
    data: "{\r

      \  \"flight_per_id\": \"1\",\r

      \  \"remark\": \"test\"\r

      }"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
