info:
  name: Suspend
  type: http
  seq: 5

http:
  method: POST
  url: "{{currentIP}}/api/FlightPermission/Suspend"
  body:
    type: json
    data: "{\r

      \  \"flight_per_id\": \"1\",\r

      \  \"remark\": \"test\"\r

      }"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
