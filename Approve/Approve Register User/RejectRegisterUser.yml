info:
  name: RejectRegisterUser
  type: http
  seq: 3

http:
  method: POST
  url: "{{currentIP}}/api/Approve/RejectRegisterUser"
  body:
    type: json
    data: "{\r

      \    \"user_id\": \"1\",\r

      \    \"remark\": \"test\"\r

      }"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
