info:
  name: ApproveRegisterUser
  type: http
  seq: 2

http:
  method: POST
  url: "{{currentIP}}/api/Approve/ApproveRegisterUser"
  body:
    type: json
    data: "{\r

      \    \"user_id\": \"136\",\r

      \    \"remark\": \"test\"\r

      }"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
