info:
  name: GetCustomerList
  type: http
  seq: 1

http:
  method: GET
  url: "{{currentIP}}/api/Customer/GetCustomerList?tokenKey={{authToken}}"
  params:
    - name: tokenKey
      value: "{{authToken}}"
      type: query
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
