info:
  name: ResetPasswordByAdmin
  type: http
  seq: 20

http:
  method: POST
  url: http://192.168.1.254:8081/api/User/ResetPasswordByAdmin
  body:
    type: json
    data: "{\r\n  \"userId\": \"2\"\r\n}"
  auth:
    type: bearer
    token: "{{rtp_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
