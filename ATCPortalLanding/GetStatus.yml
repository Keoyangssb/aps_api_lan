info:
  name: GetStatus
  type: http
  seq: 1

http:
  method: GET
  url: "{{currentIP}}/api/ATCPortalLanding/GetStatus"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
