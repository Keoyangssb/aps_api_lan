info:
  name: GetUnreadCount
  type: http
  seq: 2

http:
  method: GET
  url: "{{currentIP}}/api/Notification/GetUnreadCount"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
