info:
  name: GetUnreadNotifications
  type: http
  seq: 1

http:
  method: GET
  url: "{{currentIP}}/api/Notification/GetUnreadNotifications"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
