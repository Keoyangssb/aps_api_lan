info:
  name: MarkAsAllRead
  type: http
  seq: 4

http:
  method: PUT
  url: "{{currentIP}}/api/Notification/MarkAsAllRead"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
