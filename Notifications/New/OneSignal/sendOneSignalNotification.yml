info:
  name: sendOneSignalNotification
  type: http
  seq: 4

http:
  method: POST
  url: "{{currentIP}}/api/Notification/sendOneSignalNotification"
  body:
    type: json
    data: |-
      {
        "playerIds": ["603c647b-5cb2-4f6f-b30b-e393f61974c4"],
        "title": "APS Alert",
        "message": "Your oneSignal is ready"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
