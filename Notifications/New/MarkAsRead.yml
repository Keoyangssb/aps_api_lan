info:
  name: MarkAsRead
  type: http
  seq: 3

http:
  method: POST
  url: "{{currentIP}}/api/Notification/MarkAsRead?id=11"
  params:
    - name: id
      value: "11"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
