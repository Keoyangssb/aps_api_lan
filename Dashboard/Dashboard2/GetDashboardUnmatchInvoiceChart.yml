info:
  name: GetDashboardUnmatchInvoiceChart
  type: http
  seq: 10

http:
  method: POST
  url: "{{currentIP}}/api/Dashboard2/GetDashboardUnmatchInvoiceChart"
  body:
    type: json
    data: "{\r\n  \"year\": \"2025\"\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
