info:
  name: GetPaymentReceivedInvoice
  type: http
  seq: 2

http:
  method: POST
  url: "{{currentIP}}/api/Dashboard2/GetPaymentReceivedInvoice"
  body:
    type: json
    data: "{\r\n  \"year\": \"2025\"\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
