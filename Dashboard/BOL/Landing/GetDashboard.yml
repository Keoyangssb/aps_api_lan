info:
  name: GetDashboard
  type: http
  seq: 1

http:
  method: POST
  url: "{{currentIP}}/api/DashboardBOLLanding/GetDashboard"
  body:
    type: json
    data: |-
      {
        "year": "2026"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
