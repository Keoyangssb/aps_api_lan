info:
  name: RejectFlightMovement
  type: http
  seq: 4

http:
  method: POST
  url: "{{currentIP}}/api/StatisticOvf/RejectFlightMovement"
  body:
    type: json
    data: |-
      {
          "active_flight_plan_id": "140427",
          "remark": "test"
      }
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
